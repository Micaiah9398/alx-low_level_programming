#include "main.h"
/**
 * _isuper - function that check for uppercase character.
 * @c: input
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c);
{
	for (int c = 'A'; c <= 'Z'; c++)
		c = uppercase(c);
	putchar(c);

	return (1);

	else
		return (0);
}
