#include <stdio.h>
/**
 * main- this function is to add two integers
 *add- Add two integer
 * @a: add int a
 * @b: add int b 
 * Return: The sum of the two integers.
 */

int main()
{
	a = 20;
	b = 30;
	int result = add(a, b);

	printf("the sum of %d and %d is %d\n", a, b result);
	return sum;
